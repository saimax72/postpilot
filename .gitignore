# Never commit live credentials
app/config.php

# User uploads and runtime state
uploads/*
!uploads/.htaccess
storage/*
!storage/.htaccess

# One-time installer must not ship in a deploy
install.php

*.zip

# Design source material: reference mockups and asset packages. Processed
# copies live in assets/img/; the originals should not deploy - they are large,
# and this folder has held stray copies of app PHP files.
mockup/
